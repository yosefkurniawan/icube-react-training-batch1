import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
    todoItem: {
        display: 'flex',
    },
    todoLabel: {
        marginRight: 15,
    },
    statusActive: {
        color: 'green',
    },
    statusDone: {
        color: 'red',
    },
});

export default styles;
